services:
  - type: web
    name: koroneproxy
    env: go
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app
    startCommand: ./app
    healthCheckPath: /healthz
    envVars:
      - key: GOOS
        value: linux